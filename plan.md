这是一份为你量身定制的**电商评论与舆情分析系统**独立落地执行方案。本方案严格对齐你的实验目的和要求，不含代码，专注于**系统架构设计、核心功能拆解、算法选型、数据流转以及实验报告撰写指南**，是一份可以直接用于指导开发和写入实验报告的“产品与技术设计说明书”。

---

## 一、 系统业务场景与设计目标

本系统定位为“跨境电商/商户爆品评论与舆情智能分析系统”。

* **真实痛点**：电商卖家需要从海量买家评论中，快速提炼产品缺陷、售后问题及用户核心诉求。
* **设计目标**：构建一个基于 Web 页面的高交互 NLP 文本处理系统，集成从“单句实时分析”到“批量无监督聚类可视化”的完整链路。

---

## 二、 系统架构与技术选型

为了保证系统轻量、易开发且界面美观，整体架构采用**全栈 Python 体系**，免去前后端分离的繁琐调优：

* **表现层（前端交互）**：采用 **Streamlit** 框架。它能够用纯 Python 快速构建出具备现代设计感的响应式 Web 界面。
* **算法层（NLP 核心引擎）**：
* **分词与词性标注**：使用 **Jieba** 的精准分词与词性标注（POS）引擎。
* **命名实体识别 (NER)**：采用 **Jieba.posseg** 过滤特定词性或集成轻量级 **SpaCy (中文微型模型)**。
* **文本分类**：采用 **SnowNLP** 情绪分析引擎 + 自定义**关键词规则映射引擎**（实现多维度服务分类）。
* **高维向量化**：采用 **Scikit-learn** 的 `TfidfVectorizer`（TF-IDF 空间向量模型）。
* **无监督聚类**：采用 **Scikit-learn** 的 `KMeans` 算法。


* **数据降维与可视化**：
* **降维算法**：`PCA`（主成分分析法）。
* **交互图表**：**Plotly**（用于绘制动态语义地图）与 **PyEcharts**（用于绘制宏观仪表盘）。



---

## 三、 核心功能模块详细设计

系统共规划为三个核心交互页面，各模块功能及 strict 对齐实验要求的设计如下：

### 页面一：舆情数据看板（系统首页）

> **实验对齐**：满足“程序界面美观，易于使用”及“处理结果可视化”的要求。

1. **数据导入组件**：
* 提供文件上传控件，支持标准 `CSV` 或 `Excel` 格式的批量评论数据导入。
* 系统内置一组包含 10~20 条典型电商评论的测试数据集，确保在不上传文件时系统也能一键运行演示。


2. **核心宏观指标卡 (Metrics)**：
* **总分析评论数**：动态统计当前数据集的总量。
* **正面好评率**：统计情绪得分 $\ge 0.5$ 的评论占比。
* **负面预警数**：统计情绪极度低落（如得分 $< 0.4$）的投诉评论总数。


3. **高频词热度排行（可视化）**：
* 系统自动对批量文本进行分词，剔除中英文停用词（如“的”、“了”、“以及”等无实际意义的词）。
* 统计词频前 10 的核心词，以**条形图**或**词云图**直观展示买家近期最关注的焦点（如“屏幕”、“快递”、“客服”）。



### 页面二：单句交互分析模块

> **实验对齐**：严格对齐“输入一段话能够输出分词、命名实体识别、分类结果”的交互要求。

用户在页面的文本输入框中键入任意一条买家评论（例如：“*昨天在上海买的 iPhone，屏幕竟然碎了，气死我了！*”），点击“智能分析”按钮，系统在下方以清晰的排版实时输出三大结果：

1. **分词与词性标注**：
* 输出经过切分后的词汇序列。
* 过滤无意义符号，将名词（n）、动词（v）、形容词（a）等核心词汇带上词性标签（如：`屏幕(n)`、`碎(v)`），并用不同颜色的前端标签进行视觉隔离。


2. **命名实体识别 (NER)**：
* 从句子中精准抓取两类最关键的电商实体：
* **地名/机构名 (LOC)**：如“上海”，帮助定位售后或发货区域。
* **产品/品牌名 (PRODUCT)**：如“iPhone”，自动锁定投诉对应的核心硬件。




3. **双重文本分类**：
* **情绪极性判定（情感分类）**：通过算法输出 0~1 之间的置信度得分。接近 1 为好评，接近 0 为差评。若判定为差评，前端以闪烁的**红色警告框**提示商户。
* **业务维度归类（多标签分类）**：根据词群特征，将评论自动归入对应的商户业务板块，如：`物流配送类投诉`、`产品硬件质量投诉`、`客服售后服务反馈`。



### 页面三：批量评论聚类语义地图

> **实验对齐**：严格对齐“能够可视化多篇文章的聚类结果”这一核心难点。

针对页面一导入的多篇评论文本，进行无监督的深度主题聚类，并通过二维空间进行降维可视化：

1. **动态调参交互**：
* 提供一个滑动条（Slider），允许用户（或评审老师）手动调节要聚类的**主题数 $K$ 值**（可选 2 ~ 5 类）。系统会根据最新的 $K$ 值重新运行算法。


2. **文本特征空间构建与聚类**：
* **步骤 A（特征提取）**：将多篇文本通过 TF-IDF 算法转化为高维稀疏特征矩阵。
* **步骤 B（聚类收敛）**：将矩阵输入 K-Means 算法，自动将所有评论按语义相似度划分为 $K$ 个簇群（例如：自发将所有骂快递的归为簇 A，夸拍照好看的归为簇 B）。


3. **PCA 降维与 Plotly 动态散点图（核心亮点）**：
* 由于特征矩阵是高维的无法直接画图，利用 **PCA 算法** 提取两个主成分，将每篇评论投影到二维平面坐标 $(X, Y)$ 上。
* **高交互可视化**：在网页上渲染出一张散点图，**不同的聚类标签显示不同的颜色**。
* **鼠标悬停交互（Hover）**：当用户将鼠标悬停在图上的任何一个数据点上时，网页会自动弹出一个浮窗，**动态显示该点对应的原始买家评论文本**。这能让老师直观地看到语义相近的话在空间上聚集在一起的过程。


4. **明细下钻折叠面板**：
* 在图表下方，利用折叠组件（Expander）分门别类地列出各个聚类主题下的文本列表，方便商户批量阅读。



---

## 四、 系统数据流转全景（Data Flow）

为了方便理解系统是如何跑通的，以下是数据的完整流转逻辑：

```
【输入阶段】
   │── 用户单句输入 ──> 触发 [实时 NLP 链条] ──> 渲染 [标签与警告框]
   └── 用户批量上传 ──> 触发 [数据解析引擎]
                             │
                             ▼
【分析与核心算法阶段】
                      [批量文本数据]
                             │
                             ▼
                     [分词与停用词清洗]
                             │
                             ▼
                  [TF-IDF 空间向量化表征]
                             │
                             ▼
                    [K-Means 聚类算法] ──> 标记各文本的 [主题标签]
                             │
                             ▼
                    [PCA 主成分降维] ──> 计算出二维坐标 (X, Y)
                             │
                             ▼
【输出与可视化阶段】
                     [构建关系矩阵表]
                             │
                             ▼
              [Plotly 渲染高交互语义散点图] 
              (支持前端鼠标悬浮动态透视原始文本)

```

---

## 五、 实验报告“第四部分：实验结果及分析”撰写指南

在完成系统后，你的实验报告第四部分可以按照以下结构和分析话术来写，能极大提升报告的学术性和专业度：

### 1. 系统交互与功能验证分析

* **内容**：放置系统三个页面的运行截图。
* **分析话术**：“测试结果表明，系统具备优秀的实时交互性。当输入包含地名、产品名和强烈负面情绪的复杂复合句时，分词与词性标注模块能准确剥离核心词汇，命名实体识别模块成功抽取出‘上海’（LOC）与‘iPhone’（PRODUCT），分类模块敏感地捕捉到了差评信号并输出红色预警。各项指标达到预期，系统交互界面美观、易用。”

### 2. 文本聚类与空间降维效果评估

* **内容**：放置页面三生成的 Plotly 聚类散点图截图（最好截一张鼠标悬浮在某个点上弹出文本的图）。
* **分析话术**：“在本组实验中，批量导入了关于电商售后、物流和硬件的混合评论数据。通过 **TF-IDF 算法**捕捉文本的词频特征，并利用 **K-Means** 进行无监督分类。由于高维向量无法直接可视化，实验引入了 **PCA（主成分分析）降维技术**。
从可视化语义地图中可以观察到，相同颜色的数据点在二维空间中呈现出明显的‘簇状’聚集。通过鼠标悬停交互可以验证，空间距离较近的点，其原始文本语义高度相似（如多条投诉客服不回消息的评论自发聚集）。这表明 K-Means 配合 PCA 能够非常直观地揭示海量文本背后的隐语义主题分布，达到了极佳的可视化辅助决策效果。”

### 3. 系统局限性与改进方向思考（体现批判性思维，高分加分项）

* **分析话术**：“在本实验的基准测试中发现，传统的 TF-IDF 算法依赖关键词的显式共现，当买家使用不同的措辞表达相同意思时（例如‘速度慢’与‘派送不及时’），特征矩阵的稀疏性可能会导致少量点出现轻微偏离。在后续的实用可落地生产环境中，计划将特征提取层升级为基于预训练深度学习模型的 **Dense Embedding（稠密向量）**，以获得更深层次的上下文语义捕捉能力。”